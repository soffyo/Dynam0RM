{
  "name": "dynam0rx",
  "version": "1.0.0",
  "description": "AWS DynamoDB Typescript ORM Client",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "git": "git add . && git commit -m",
    "postgit": "git push",
    "version": "git add .",
    "postversion": "git push",
    "prepublishOnly": "npm run test",
    "test": "jest",
    "dynamodb": "cd dynamodb_local && java -Djava.library.path=./DynamoDBLocal_lib -jar ./DynamoDBLocal.jar -inMemory",
    "test-db": "concurrently -P -k -s \"last\" -n \"DynamoDB,Jest\" -c \"bgBlue.bold.white,bgRed.bold.white\" \"npm:dynamodb\" \"sleep 1 && jest {@}\" --"
  },
  "keywords": [
    "aws",
    "dynamodb",
    "typescript",
    "client"
  ],
  "author": "soffyo",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "@types/node": "^18.8.3",
    "concurrently": "^7.4.0",
    "jest": "^29.1.2",
    "ts-jest": "^29.0.3",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.186.0",
    "@aws-sdk/lib-dynamodb": "^3.186.0",
    "reflect-metadata": "^0.1.13"
  }
}
